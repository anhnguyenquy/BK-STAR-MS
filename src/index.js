import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import App from './App';

//Render the <App/> component into the div with id='root' into the body of the index.html file
ReactDOM.render(
  <App/>,
  document.getElementById('root')
);

